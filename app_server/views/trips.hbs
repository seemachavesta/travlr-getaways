<h1>{{title}}</h1>

<ul>
  {{#each trips}}
    <li>
      <strong>{{title}}</strong> — {{duration}} — ${{price}}
      <p>{{description}}</p>
    </li>
  {{/each}}
</ul>

<p><a href="/">Back to home</a></p>
